# 网络会话监控配置文件

# 采样间隔（秒）
sample_interval: 5


# 日志配置
log:
  path: "./monitor.log"
  rotation_days: 7
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR

# Web 服务配置
web:
  host: "0.0.0.0"
  port: 5000
  debug: false

# 国际化
language: "zh-CN"  # zh-CN, en-US
